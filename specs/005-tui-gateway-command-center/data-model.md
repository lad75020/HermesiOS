# Data Model: TUI Gateway and Command Center

## TUI Workspace

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Gateway Event

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Interactive Prompt

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Command Run

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.
