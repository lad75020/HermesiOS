# Data Model: Agent Runtime Configuration

## Runtime Panel

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Companion Registry Item

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Configuration Change

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.

## Operational Target

**Fields**:
- `id`: Stable identity.
- `displayState`: User-visible state.
- `source`: Scoped feature source or host-backed origin.

**Validation Rules**:
- State is clear, non-secret, and recoverable.
- Invalid or unavailable backing data produces an actionable user-visible state.
